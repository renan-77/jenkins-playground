pipeline {

    agent any

    stages {
        stage("trigger"){
            steps {
                echo 'Trigerring'
            }
        }

        stage("build"){
            steps {
                echo 'Building'
            }
        }

        stage("deploy"){
            steps {
                echo 'Deploying'
            }
        }
    }
}